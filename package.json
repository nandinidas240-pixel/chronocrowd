{
  "name": "chronocrowd-stadium-command-center",
  "version": "4.0.0",
  "description": "ChronoCrowd — Cricket World Finals Stadium AI Command Center",
  "private": true,
  "scripts": {
    "test": "jest --testEnvironment jsdom",
    "test:watch": "jest --watch --testEnvironment jsdom",
    "test:coverage": "jest --coverage --testEnvironment jsdom"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/*.test.js"],
    "verbose": true
  }
}
